You are a feature analysis expert for table field classification.

You are given **multiple table fields** from the same table. Analyze ALL fields in one go. For each field, extract salient features that will help with accurate classification.

{% if table_context %}
## Table Context (applies to ALL fields):
The whole table has been analyzed:
- Business scenario: {{ table_context.business_scenario }}
- Key business concepts: {{ table_context.key_business_concepts|join(', ') }}
- Core business objects: {{ table_context.core_business_objects|join(', ') }}
- Overall description: {{ table_context.overall_description }}
- Table type/role: {{ table_context.table_type }}

Use this table-level context to help you understand what each field is about.
{% endif %}

## All Fields to Analyze:

{% for input in inputs %}
### Field {{ loop.index }}:
Table name: {{ input.table_name }}
Field name: {{ input.field_name }}
{% if input.field_description %}Field description: {{ input.field_description }}{% else %}No field description provided{% endif %}

{% endfor %}

## Available classification hierarchy (for reference):
{% for cat in hierarchical_categories %}
- {{ cat.level1 }} > {{ cat.level2 }} > {{ cat.data_item }}
{% for sub in cat.data_subitems %}  - subitem: {{ sub.name }}{% if sub.description %}: {{ sub.description }}{% endif %}
{% endfor %}{% endfor %}

## Your task:

For **EACH FIELD** above, perform the following analysis:
1. What keywords do you extract from the TABLE NAME? What business context does the table name suggest?
2. What keywords/word roots do you extract from the FIELD NAME? Split camel_case/snake_case into meaningful words.
3. What additional keywords/information do you get from the field description (if provided)?
4. Give a brief semantic summary: what business concept does this field most likely describe?
5. Analyze consistency: are the information from table name, field name, and description consistent with each other? Are there any conflicts or ambiguities?
6. Which source (table_name, field_name, or description) do you think should be given more weight for classification in this case?

## Output format (JSON only):

{
  "field_analyses": [
    {
      "table_name_keywords": ["keyword1", "keyword2", ...],
      "field_name_keywords": ["keyword1", "keyword2", ...],
      "description_keywords": ["keyword1", "keyword2", ...],
      "semantic_summary": "brief description of what this field is about",
      "consistency_analysis": "analysis of whether different sources agree",
      "dominant_source": "table_name|field_name|description"
    },
    ... (one entry for each field above, in the SAME ORDER as input)
  ]
}

Note:
- You must output exactly one entry in `field_analyses` for each input field.
- Keep the same order as the input fields.
- dominant_source must be exactly one of "table_name", "field_name", or "description" for each field.
